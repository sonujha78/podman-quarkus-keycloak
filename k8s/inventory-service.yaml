# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-5.7.0
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2026-09-22T08:51:14Z"
  labels:
    app: inventory-service-pod
  name: inventory-service-pod
spec:
  containers:
  - image: localhost/inventory-service:native
    name: inventory-service
    imagePullPolicy: Never
    ports:
    - containerPort: 8080
      hostPort: 8080
    securityContext:
      runAsNonRoot: true

